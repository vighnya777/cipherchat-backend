# Docker Guide

Docker files are not present in this repository. Do not containerize the current in-memory application for production until PostgreSQL, Redis, durable media storage, and Socket.IO scaling are implemented. A future Docker deployment should run the web service, PostgreSQL, Redis, a background worker, and a reverse proxy as separate services.
